submit signup
hash password
save signup info + hash to DB


sign in again
check for username
if user exists, hash pswd they're trying to log in with
next, check that hash against the hash for the existing username

if same, log the user in and start a session so they're logged in on every page.